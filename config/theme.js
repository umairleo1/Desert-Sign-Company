import * as colors from './colors';

export const themeLight = {
  dark: false,
  colors: {
    // primary: colors.blue,
    primary: colors.blue,
    background: colors.white,
    secondary: colors.lightGreen,
    button: colors.sky,
    signupButton: colors.navy,
    placeholder: colors.gray,
  },
};
